# 🔧 КОМПЛЕКСНЫЙ ПРОМПТ ДЛЯ ДОРАБОТКИ СИСТЕМЫ УЧЁТА ОБОРУДОВАНИЯ

## Контекст проекта

приложение учёта оборудования с тремя подсистемами (CUTE, FIDS, Zamar). Проект реализован на 10+ шагов плана развития. Нужна комплексная доработка функционала.

**Основная архитектура:**
- Три системы: CUTE, FIDS, Zamar (каждая со своими Equipment и Installation моделями)
- Модели оборудования содержат: тип, модель, инвентарный номер, **серийный номер**, статус, место установки, примечание
- Места установки имеют привязку к терминалам (enum A–F)
- Реализованы: дашборд, фильтры, экспорт в CSV/Excel, audit log
- Уже добавлены типы оборудования для CUTE и других систем

---

## ЧАСТЬ 1: ДОРАБОТКА ФОРМ ОБОРУДОВАНИЯ И СИНХРОНИЗАЦИЯ СЕРИЙНЫХ НОМЕРОВ

### 1.1 Обязательное поле «Серийный номер» во всех формах

**Требование:** Поле `serial_number` должно быть **обязательным** при добавлении и редактировании оборудования во всех трёх системах (CUTE, FIDS, Zamar).

Реализация:
- Обновить моделью CuteEquipment, FidsEquipment, ZamarEquipment валидациями:
  ```ruby
  validates :serial_number, presence: true, uniqueness: { scope: [:type] }
  # Или без scope, если серийные номера должны быть уникальными в целом
  ```
- Обновить формы (views) добавления и редактирования оборудования:
  - Поле для ввода серийного номера должно быть отмечено как required в HTML
  - Добавить валидацию на фронте (JavaScript) и на бэке
  - Показывать ошибку валидации под полем, если пользователь попытается сохранить без серийного номера
- Обновить seeds/fixtures, если есть данные для тестирования (добавить серийные номера)
- Если в БД есть старые записи без серийного номера — предложить миграцию с дефолтным значением или обработать вручную

---

### 1.2 Модальное окно для привязки оборудования к месту установки (CUTE)

**Место в интерфейсе:** Карточка места установки CUTE → раздел "Оборудование на этом месте" → кнопка "Привязать"

**Функционал:**

1) **Модальное окно «Привязать оборудование»:**
   - Заголовок: "Привязать оборудование к [название места установки]"
   - Поле ввода: "Серийный номер оборудования" (текстовое поле, может быть с автодополнением)
   - При вводе серийного номера:
     * Запрос к серверу (AJAX)
     * Поиск оборудования в CuteEquipment по serial_number
     * **Условие:** Оборудование должно быть **не привязано ни к какому месту установки** (installation_id == null или installation_id пуст)
     * Если оборудование найдено:
       - Вывести информацию об оборудовании (как "previewed card"):
         * Тип оборудования
         * Модель
         * Инвентарный номер
         * Серийный номер (как подтверждение)
         * Текущий статус (должен быть «Готово к выдачи» или другой логичный для привязки)
       - Кнопка «Привязать» (зелёная, активная)
     * Если оборудование не найдено:
       - Вывести сообщение: "Оборудование не найдено или уже привязано к месту установки"
       - Кнопка «Привязать» неактивна (disabled)

2) **Логика привязки (валидация для обычных пользователей):**
   - **Правило для CUTE:** К месту установки CUTE можно привязать **только одно оборудование одного типа**
     * Например, к Стойке 312 можно привязать:
       - 1 Принтер посадочных талонов
       - 1 Сканер
       - 1 Монитор
       - ...но НЕ два Принтера посадочных талонов
   - При попытке привязать второе оборудование того же типа:
     * Проверка перед отправкой на сервер (если возможно, AJAX)
     * Блокирующее сообщение: "К этому месту установки уже привязано оборудование типа [Тип]. Удалите предыдущее оборудование перед привязкой нового."
   - **Исключение для администраторов:** Администраторы могут привязывать **неограниченное количество** оборудования, включая несколько одного типа
     * Флаг/роль для определения администратора (используй существующую систему ролей из проекта)

3) **Обновление после привязки:**
   - После успешной привязки:
     * Модальное окно закрывается
     * Статус оборудования автоматически меняется на «В работе» (или другой логичный статус)
     * Таблица "Оборудование на этом месте" на странице места установки обновляется (добавляется строка с новым оборудованием)
     * В audit log записывается: [ФИ пользователя, дата, время] — "Привязано оборудование [Модель] (S/N: [Серийный номер]) к месту установки [Название]"

4) **UI/UX:**
   - Модальное окно с Tailwind CSS
   - Поле ввода серийного номера с фокусом (автофокус при открытии)
   - Spinner/loader при поиске
   - Clear error/success сообщения
   - Кнопка «Отмена» и кнопка «Привязать»
   - Ширина modal примерно 500-600px (medium)

---

## ЧАСТЬ 2: ДОРАБОТКА ИСТОРИИ ИЗМЕНЕНИЙ (AUDIT LOG)

### 2.1 Колонка «ОБЪЕКТ» — понятные наименования

**Текущее состояние:** Вероятно, в audit log хранится код/id объекта

**Требование:** Колонка "ОБЪЕКТ" должна выводить **понятное человеческое описание**:
- Для оборудования: "Тип/Модель (Серийный номер)"
  * Пример: "Принтер посадочных талонов / Brother QL-820NWb (SN123456)"
  * Пример: "Сканер / Zebra DS3678 (SN987654)"
- Для мест установки: "Название места установки (Терминал X)"
  * Пример: "Стойка регистрации 312 (Терминал B)"
  * Пример: "Гейт 12 (Терминал A)"

Реализация:
- В модели AuditLog добавить методы для получения описаний:
  ```ruby
  def object_description
    case auditable_type
    when 'CuteEquipment', 'FidsEquipment', 'ZamarEquipment'
      equipment = auditable
      "#{equipment.equipment_type} / #{equipment.model} (S/N: #{equipment.serial_number})"
    when 'CuteInstallation', 'FidsInstallation', 'ZamarInstallation'
      installation = auditable
      terminal = installation.terminal ? "Терминал #{installation.terminal}" : "Без терминала"
      "#{installation.name} (#{terminal})"
    else
      auditable_type
    end
  end
  ```
- В view (таблице истории) использовать:
  ```erb
  <%= audit_log_entry.object_description %>
  ```

---

### 2.2 Колонка «ИЗМЕНЕНИЯ» — понятный и читаемый формат

**Текущее состояние:** Вероятно, выводится raw JSON или неформатированная строка с кодами полей

**Требование:** Колонка "ИЗМЕНЕНИЯ" должна выводиться в **понятном, читаемом виде**:

Примеры желаемого вывода:
```
Статус: В работе → Ожидается из ремонта
Место установки: Стойка 312 (Терминал B) → Стойка 401 (Терминал C)
Примечание: Требуется чистка → Экран неисправен
Модель: Brother QL-820NWb → Zebra QL-820
```

Реализация:
1) В модели AuditLog добавить метод для форматирования изменений:
   ```ruby
   def formatted_changes
     return "" if changes.blank?
     
     changes.map do |field, (old_value, new_value)|
       field_label = human_field_name(field)
       old_display = format_field_value(field, old_value)
       new_display = format_field_value(field, new_value)
       "#{field_label}: #{old_display} → #{new_display}"
     end.join("; ")
   end
   
   private
   
   def human_field_name(field)
     case field
     when 'status' then 'Статус'
     when 'model' then 'Модель'
     when 'serial_number' then 'Серийный номер'
     when 'installation_id' then 'Место установки'
     when 'notes' then 'Примечание'
     else field.titleize
     end
   end
   
   def format_field_value(field, value)
     case field
     when 'status'
       value.present? ? I18n.t("status.#{value}") : '(не установлено)'
     when 'installation_id'
       if value.blank?
         '(не привязано)'
       else
         installation = find_installation_by_id(value)
         installation&.name || "(Место ##{value})"
       end
     else
       value.present? ? value.to_s : '(пусто)'
     end
   end
   ```

2) В view используй:
   ```erb
   <%= audit_log_entry.formatted_changes %>
   ```

3) Стилизация в CSS:
   - Старое значение: серый текст или зачёркнутое
   - Стрелка: визуально выделенная (→ или иконка)
   - Новое значение: зелёный текст или выделено

---

## ЧАСТЬ 3: РАЗДЕЛ РЕМОНТ

### 3.1 Список оборудования в статусе «В ремонт»

**Раздел:** Главное меню → "Ремонт" (новый пункт)

**Список:**
- Объединённый список всего оборудования (CUTE + FIDS + Zamar) со статусом "В ремонт"
- Таблица с колонками:
  * ☑️ (Чекбокс для выбора)
  * Система (CUTE / FIDS / Zamar)
  * Терминал
  * Место установки
  * Тип оборудования
  * Модель
  * Инвентарный номер
  * Серийный номер
  * Статус (будет всегда "В ремонт" на этой странице)
  * Примечание
  * Дата последнего действия
  * ФИ пользователя, который последний раз действовал с оборудованием

### 3.2 Фильтры в разделе Ремонт

Панель фильтров:
- **По терминалу:** Dropdown с вариантами A–F, "Все"
- **По типу оборудования:** Dropdown (динамически формируется из оборудования, которое есть в статусе "В ремонт")
  * Для CUTE: "Принтер посадочных талонов", "Сканер", и т.д.
  * Для FIDS/Zamar: их типы
  * Вариант "Все типы"
- **По системе (опционально):** CUTE / FIDS / Zamar / Все системы
- **Кнопка «Сбросить фильтры»**

Реализация:
- Использовать Ransack для фильтрации
- Сохранять фильтры в session/params для пагинации

### 3.3 Массовая операция «Оформить акт передачи в ремонт»

**Процесс:**

1) Пользователь отмечает чекбоксами оборудование, которое нужно отправить в ремонт
2) Нажимает кнопку **"Оформить акт передачи в ремонт"** (активна только если выбрано ≥1 оборудования)
3) Открывается модальное окно подтверждения:
   - "Вы уверены, что хотите оформить акт передачи в ремонт для [N] единиц оборудования?"
   - Краткий список выбранного оборудования (первые 5, остальное "и ещё X")
   - Кнопки: "Отмена" и "Подтвердить"
4) После подтверждения:
   - **Для каждого выбранного оборудования:**
     * Статус меняется с "В ремонт" на "Ожидается из ремонта"
     * Записывается audit log запись: "Статус: В ремонт → Ожидается из ремонта (передано в ремонт, акт №[номер])"
   - **Создаётся запись RepairBatch (или RepairAct):**
     * repair_number (автогенерируемый номер, например, REP-2026-001)
     * created_at (дата/время)
     * created_by_user_id (пользователь, создавший акт)
     * equipment_count (количество единиц)
   - **Связь оборудование ↔ RepairBatch:**
     * Добавить поле repair_batch_id в модели Equipment или создать join-таблицу RepairBatchItems
5) После успешного создания акта:
   - Вывести сообщение: "✓ Акт №[номер] создан! [N] единиц оборудования отправлено в ремонт."
   - Таблица обновляется (оборудование исчезает из списка "В ремонт")
   - Предложить ссылку на просмотр/экспорт только что созданного акта

### 3.4 Просмотр и экспорт акта в Excel

**Страница акта:**
- Информация об акте:
  * Номер акта: REP-2026-001
  * Дата создания: 25 Января 2026, 14:32
  * Создал: Иван Петров (ФИ и роль)
  * Статус: Отправлено (или другой статус)
  * Количество оборудования: 47 единиц

- Таблица оборудования в акте с колонками:
  * Система
  * Терминал
  * Тип оборудования
  * Модель
  * Инвентарный номер
  * Серийный номер
  * Примечание

- **Кнопка «Экспорт в Excel»:**
  * Генерирует XLSX файл с названием вида "Акт_ремонта_REP-2026-001.xlsx"
  * Файл содержит:
    - Header с информацией об акте (номер, дата, создал)
    - Таблица с оборудованием (как выше)
    - Footer с итоговым количеством

---

### 3.5 История ремонта

**Раздел:** Ремонт → "История ремонта" (вкладка или подраздел)

**Функционал:**

1) **Модель RepairHistory (или используй audit logs с фильтрацией):**
   - Хранит информацию о каждом событии отправки в ремонт:
     * repair_batch_id (ссылка на акт)
     * equipment_id, equipment_type (polymorphic)
     * system (CUTE / FIDS / Zamar)
     * serial_number (денормализовано для быстрого поиска)
     * model
     * terminal (на момент отправки)
     * sent_to_repair_at (дата)
     * equipment_count
     * user_id (кто отправил)

2) **Интерфейс истории:**
   - Таблица с колонками:
     * Система
     * Модель
     * Серийный номер
     * Терминал (на момент отправки)
     * Дата отправки в ремонт
     * Номер акта
     * ФИ пользователя, который отправил

3) **Фильтры:**
   - **По серийному номеру (точный поиск):**
     * Введи серийный номер — и вывести все записи по этому конкретному оборудованию
     * Показать статистику для этого оборудования:
       - Сколько раз было отправлено в ремонт
       - Даты всех отправок
   - **По диапазону дат:**
     * От даты и До даты (дата отправки в ремонт)
   - **По системе:** CUTE / FIDS / Zamar
   - **По модели оборудования**
   - **По терминалу**

4) **Аналитика и статистика:**
   - Блок агрегированной статистики внизу/наверху:
     * "Всего отправлено в ремонт (за период): [N] единиц"
     * "Уникальных серийных номеров: [M]"
     * "Топ-3 моделей по частоте ремонтов:" (таблица с моделью и количеством)
   - При фильтрации по серийному номеру — таймлайн для этого оборудования:
     * 22 Января 2026, 14:30 — Акт REP-2026-001
     * 15 Января 2026, 10:15 — Акт REP-2026-000
     * ...и т.д.

---

## ЧАСТЬ 4: ДОРАБОТКА ДАШБОРДА

### 4.1 Блок «Ремонт» на главной странице

Добавить новую карточку/блок:

**Название:** "Ремонт"
**Содержимое:**
- Два ключевых показателя в одном блоке:
  * **"В ремонт"** — количество оборудования, находящегося в статусе "В ремонт" (CuteEquipment.where(status: :in_repair).count + FIDS + Zamar)
  * **"Ожидается из ремонта"** — количество оборудования, находящегося в статусе "Ожидается из ремонта"
- Подписи:
  * "Подготовлено к отправке в ремонт"
  * "Ожидается возврата из ремонта"
- Карточка кликабельна — по клику переход в раздел "Ремонт"
- Цветовой акцент (оранжевый или красный, чтобы выделился)
- Иконка (например, wrench/gear)

Расположение на дашборде:
- После общей статистики (общее кол-во оборудования и по системам)
- Перед детальными таблицами
- Желательно, в одной строке с другими агрегатами

### 4.2 Доработка блока «На складе (без привязки к месту установки)»

**Текущее состояние:** Вероятно, есть блок, показывающий общее количество оборудования на складе

**Требование:** Добавить детальную разбивку по **моделям оборудования и терминалам**

**Структура:**
- Основной блок остаётся: название и общее число
- Внутри добавить разворачивающуюся/постоянно видимую таблицу:
  * **Для каждой системы (CUTE, FIDS, Zamar) отдельная таблица:**
    - Колонки: Модель | Терминал | Количество
    - Если терминал не задан — "Без терминала"
    - Пример:
      ```
      CUTE — На складе
      Модель                          | Терминал | Количество
      Brother QL-820NWb               | A        | 3
      Brother QL-820NWb               | B        | 2
      Zebra DS3678 (сканер)          | -        | 1
      ...
      
      FIDS — На складе
      ...
      
      Zamar — На складе
      ...
      ```

**Логика:**
- CuteEquipment, где installation_id = null (или другой критерий для склада)
- .group(:model, :terminal) (или через join с installation, если терминал там)
- .count → получить структуру данных
- Отобразить в таблице(s)

### 4.3 Новый блок «Количество оборудования по моделям (всего)»

**Название:** "Оборудование по моделям" или "Всё оборудование по моделям"

**Содержимое:**
- Для **каждой системы (CUTE, FIDS, Zamar)** отдельная таблица:
  * Колонки: Модель | Количество
  * Сортировка по убыванию количества (самые массовые модели наверху)
  * Пример:
    ```
    CUTE
    Модель                        | Количество
    Brother QL-820NWb             | 25
    Zebra DS3678                  | 18
    Bematech MP2100 TH            | 12
    Fujitsu ScanSnap iX1500       | 8
    ...
    
    FIDS
    ...
    
    Zamar
    ...
    ```

**Логика:**
- CuteEquipment.group(:model).count
- FidsEquipment.group(:model).count
- ZamarEquipment.group(:model).count
- Построить структуру данных и вывести в views

**Расположение на дашборде:**
- Можно поместить в конце дашборда (после детальных таблиц типов и терминалов)
- Или в отдельной секции с табами по системам

---

## ЧАСТЬ 5: ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ И ОПТИМИЗАЦИЯ

### 5.1 Контроллеры и маршруты

**Новые/обновлённые контроллеры:**

1) **RepairsController** (главный для раздела "Ремонт"):
   - `index` — список оборудования в статусе "В ремонт" с фильтрами
   - `create_batch` (POST) — оформить акт передачи в ремонт
   - `show` (для RepairBatch) — просмотр акта
   - `export_batch_to_excel` — экспорт акта в Excel

2) **Repairs::HistoryController**:
   - `index` — история ремонта с фильтрами и аналитикой
   - AJAX endpoint для дополнительной фильтрации/статистики (опционально)

3) **DashboardController** (обновить):
   - `index` — обновить логику расчёта всех новых блоков

4) **EquipmentController** (для всех систем):
   - Обновить методы создания/редактирования для валидации serial_number

5) **InstallationsController** (для CUTE, FIDS, Zamar):
   - Добавить AJAX endpoint для поиска оборудования по serial_number при привязке
   - Логика валидации (проверка дублей типов для CUTE)

**Маршруты (routes.rb):**
```ruby
namespace :repairs do
  resources :batches, only: [:index, :show] do
    member do
      post :create_batch
      get :export_to_excel
    end
  end
  resources :histories, only: [:index]
end

# или, если ремонт — топ-левел раздел:
resources :repairs do
  collection do
    post :create_batch
  end
  member do
    get :export_to_excel
  end
end
```

### 5.2 Модели и миграции

**Новые модели/таблицы:**

1) **RepairBatch** (или RepairAct):
   ```ruby
   create_table :repair_batches do |t|
     t.string :repair_number, null: false, unique: true
     t.references :user, null: true  # Создавший акт
     t.integer :equipment_count, default: 0
     t.string :status, default: 'sent'  # sent, in_progress, received, closed
     t.datetime :created_at
     t.datetime :updated_at
   end
   ```

2) **RepairBatchItem** (если нужна связь many-to-many):
   ```ruby
   create_table :repair_batch_items do |t|
     t.references :repair_batch, null: false
     t.string :equipment_type  # CuteEquipment, FidsEquipment, ZamarEquipment
     t.bigint :equipment_id
     t.string :system  # CUTE, FIDS, Zamar
     t.string :serial_number
     t.string :model
     t.string :terminal
     t.datetime :created_at
   end
   ```

3) **RepairHistory** (если не используются audit logs):
   ```ruby
   create_table :repair_histories do |t|
     t.references :repair_batch, null: true
     t.string :equipment_type
     t.bigint :equipment_id
     t.string :serial_number, null: false
     t.string :model
     t.string :system
     t.string :terminal
     t.datetime :sent_to_repair_at, null: false
     t.references :user, null: true
     t.timestamps
   end
   ```

**Обновить существующие модели:**
- Добавить `has_many :repair_batches` или связь в Equipment моделях
- Добавить валидации к serial_number (presence, uniqueness в зависимости от требований)
- Добавить методы для определения "на складе" (scope :without_installation)

### 5.3 Индексы для оптимизации

Добавить индексы для быстрых запросов:
```ruby
add_index :cute_equipments, :status
add_index :cute_equipments, :serial_number
add_index :cute_equipments, [:equipment_type, :installation_id]
add_index :cute_equipments, :model
add_index :cute_installations, :terminal

# Аналогично для FIDS и Zamar

add_index :repair_batches, :repair_number, unique: true
add_index :repair_batch_items, [:equipment_type, :equipment_id]
add_index :repair_histories, :serial_number
add_index :repair_histories, [:system, :sent_to_repair_at]

add_index :audit_logs, [:auditable_type, :auditable_id]
```

### 5.4 Валидация и безопасность

1) **Проверка ролей:**
   - Обычные пользователи: не могут привязывать более одного оборудования одного типа к месту установки
   - Администраторы: нет ограничений
   - Реализовать через Pundit или CanCanCan

2) **CSRF защита:**
   - Все формы должны иметь CSRF токены
   - AJAX запросы должны передавать токен

3) **Валидация на фронте и бэке:**
   - Обязательность serial_number
   - Уникальность (если требуется)
   - Проверка дублей при привязке оборудования

---

## ЧАСТЬ 6: UI/UX И TAILWIND CSS

### 6.1 Общие требования к стилизации

- **Цветовая схема:**
  * Основной цвет: используемый в проекте (вероятно, teal/cyan)
  * Ремонт: оранжевый/красный акцент (warning)
  * Успешные действия: зелёный
  * Ошибки: красный
  * Информация: серый/синий

- **Компоненты:**
  * Карточки: `rounded-lg`, `shadow-md`, `p-6`, `border`
  * Таблицы: sticky header, hover effects
  * Модальные окна: overlay с закрытием по Escape и клику снаружи
  * Кнопки: `py-2 px-4`, `rounded`, `transition`
  * Inputs: `p-2`, `border`, `rounded`, `focus:ring`

### 6.2 Страницы, требующие стилизации

1. **Страница списка "Ремонт"** — таблица с фильтрами и чекбоксами
2. **Модальное окно привязки оборудования** — с preview информации
3. **Страница акта** — информация об акте + таблица оборудования
4. **Страница истории ремонта** — таблицы и аналитика
5. **Дашборд** — новые блоки (ремонт, склад, модели)

---

## ЧАСТЬ 7: ПРИМЕРЫ КОДА И РЕАЛИЗАЦИИ

### 7.1 Пример привязки оборудования (AJAX)

**View:**
```erb
<!-- app/views/cute_installations/show.html.erb -->

<div id="equipment-section">
  <h3>Оборудование на этом месте</h3>
  
  <% if @installation.cute_equipments.any? %>
    <table class="min-w-full border">
      <!-- таблица оборудования -->
    </table>
  <% else %>
    <p class="text-gray-500">Оборудование не привязано</p>
  <% end %>
  
  <button class="btn btn-primary mt-4" data-action="click->modal#open" 
          data-modal-id="attach-equipment">
    Привязать оборудование
  </button>
</div>

<!-- Modal -->
<div id="attach-equipment" class="modal hidden">
  <div class="modal-overlay"></div>
  <div class="modal-content rounded-lg bg-white p-6 w-full max-w-md">
    <h2 class="text-xl font-bold mb-4">Привязать оборудование</h2>
    
    <div class="form-group mb-4">
      <label for="serial_number">Серийный номер</label>
      <input type="text" id="serial_number" 
             data-action="input->equipment-search#search"
             placeholder="Введите серийный номер..."
             class="form-input w-full">
    </div>
    
    <div id="search-result" class="mb-4 hidden">
      <!-- Preview найденного оборудования -->
      <div class="p-4 bg-gray-50 rounded border">
        <p><strong>Тип:</strong> <span id="equipment-type"></span></p>
        <p><strong>Модель:</strong> <span id="equipment-model"></span></p>
        <p><strong>Серийный номер:</strong> <span id="equipment-sn"></span></p>
      </div>
    </div>
    
    <div id="search-error" class="mb-4 hidden text-red-600">
      <!-- Ошибка поиска -->
    </div>
    
    <div class="flex justify-end gap-3">
      <button class="btn btn-secondary" data-action="click->modal#close">Отмена</button>
      <button id="attach-btn" class="btn btn-primary" disabled>Привязать</button>
    </div>
  </div>
</div>
```

**Controller:**
```ruby
# app/controllers/cute_installations_controller.rb

def search_equipment
  serial_number = params[:serial_number]
  equipment = CuteEquipment.find_by(
    serial_number: serial_number, 
    cute_installation_id: nil
  )
  
  if equipment
    # Проверка для CUTE: есть ли уже оборудование этого типа в месте установки
    if equipment_type_exists?(@installation, equipment.equipment_type)
      render json: {
        success: false,
        error: "К этому месту уже привязано оборудование типа #{equipment.equipment_type}"
      }, status: :unprocessable_entity
    else
      render json: {
        success: true,
        equipment: {
          id: equipment.id,
          equipment_type: equipment.equipment_type,
          model: equipment.model,
          serial_number: equipment.serial_number
        }
      }
    end
  else
    render json: {
      success: false,
      error: "Оборудование не найдено или уже привязано"
    }, status: :not_found
  end
end

def attach_equipment
  equipment = CuteEquipment.find(params[:equipment_id])
  
  # Финальная проверка на CUTE
  if equipment_type_exists?(@installation, equipment.equipment_type)
    return render json: { error: "Дублирование типов не допускается" }, status: :unprocessable_entity
  end
  
  equipment.update(cute_installation_id: @installation.id, status: :in_use)
  
  # Audit log
  AuditLog.create(
    auditable: equipment,
    user: current_user,
    action: 'attach',
    changes: {
      installation_id: [nil, @installation.id],
      status: [:unattached, :in_use]
    }
  )
  
  render json: { success: true, message: "Оборудование привязано" }
end

private

def equipment_type_exists?(installation, equipment_type)
  return false if current_user.admin?
  installation.cute_equipments.where(equipment_type: equipment_type).exists?
end
```

### 7.2 Пример истории ремонта в audit log

```ruby
# Форматирование для вывода в таблице

def object_description
  case auditable_type
  when 'CuteEquipment'
    eq = auditable
    "#{eq.equipment_type} / #{eq.model} (S/N: #{eq.serial_number})"
  when 'CuteInstallation'
    inst = auditable
    "#{inst.name} (Терминал #{inst.terminal})"
  else
    auditable_type
  end
end

def formatted_changes
  return "" if changes.blank?
  
  changes.map do |field, (old_val, new_val)|
    label = human_field_name(field)
    old_display = format_value(field, old_val)
    new_display = format_value(field, new_val)
    "#{label}: #{old_display} → #{new_display}"
  end.join("; ")
end

def human_field_name(field)
  {
    'status' => 'Статус',
    'model' => 'Модель',
    'serial_number' => 'Серийный номер',
    'installation_id' => 'Место установки',
    'notes' => 'Примечание',
    'cute_installation_id' => 'Место установки (CUTE)',
    'fids_installation_id' => 'Место установки (FIDS)',
    'zamar_installation_id' => 'Место установки (Zamar)'
  }[field] || field.titleize
end

def format_value(field, value)
  case field
  when 'status'
    value.present? ? I18n.t("activerecord.enums.cute_equipment.status.#{value}") : '(не установлено)'
  when /installation_id/
    if value.blank?
      '(не привязано)'
    else
      installation = find_installation(value)
      installation&.display_name || "(Место ##{value})"
    end
  else
    value.present? ? value.to_s : '(пусто)'
  end
end
```

---

## ЧАСТЬ 8: ЧЕКЛИСТ РЕАЛИЗАЦИИ

Следующие пункты нужно реализовать в указанном порядке:

- [ ] **Шаг 1:** Миграция БД — добавить serial_number (обязательное поле) и индексы
- [ ] **Шаг 2:** Валидация serial_number в моделях оборудования
- [ ] **Шаг 3:** Обновить формы добавления/редактирования оборудования (все три системы)
- [ ] **Шаг 4:** Контроллер и views для поиска оборудования по serial_number (AJAX)
- [ ] **Шаг 5:** Модальное окно привязки оборудования (со валидацией для CUTE)
- [ ] **Шаг 6:** Обновить audit log для понятного вывода в истории изменений
- [ ] **Шаг 7:** Миграция для RepairBatch и RepairBatchItem (или RepairHistory)
- [ ] **Шаг 8:** Создать раздел "Ремонт" — список, фильтры, массовые операции
- [ ] **Шаг 9:** Оформление акта передачи в ремонт и смена статуса
- [ ] **Шаг 10:** Экспорт акта в Excel
- [ ] **Шаг 11:** Создать раздел "История ремонта" с фильтрами и аналитикой
- [ ] **Шаг 12:** Обновить дашборд — новые блоки (ремонт, склад, модели)
- [ ] **Шаг 13:** Tailwind CSS стилизация всех новых компонентов
- [ ] **Шаг 14:** Тестирование (RSpec, feature tests)
- [ ] **Шаг 15:** Документация и git commits на русском языке

---

## ЧАСТЬ 9: ДОПОЛНИТЕЛЬНЫЕ РЕКОМЕНДАЦИИ

### 9.1 Использование существующих gems

- **Ransack** — для поиска и фильтрации (уже в проекте?)
- **Kaminari** или **Pagy** — для пагинации
- **Devise** — для аутентификации (уже в проекте)
- **Pundit** или **CanCanCan** — для авторизации (проверка ролей при привязке)
- **Axlsx** или **Caxlsx** — для экспорта в Excel (если не используется)
- **Audited** или **PaperTrail** — для audit logging (если свой audit log, то оставить как есть)

### 9.2 Возможные расширения в будущем

- История возврата оборудования из ремонта
- Интеграция с внешней системой управления ремонтом
- Уведомления при отправке/получении из ремонта
- Прогноз сроков ремонта на основе истории
- Экспорт истории ремонта в различные форматы

---

## ИТОГО

Это комплексный промпт, который объединяет:

✅ **Часть 1** — Обязательное поле serial_number + модальная привязка оборудования для CUTE
✅ **Часть 2** — Понятная история изменений в audit log
✅ **Часть 3** — Полный раздел "Ремонт" (список, фильтры, акты, история)
✅ **Часть 4** — Доработка дашборда (блок ремонта, склад, модели)
✅ **Часть 5-9** — Технические детали, UI/UX, чеклист и рекомендации

**Можешь передать этот промпт Claude Opus 4.5 целиком, и он выдаст тебе готовый код для всех этих компонентов.**

Успехов в разработке! 🚀