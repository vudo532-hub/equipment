<div class="min-h-screen flex items-center justify-center bg-gray-100 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div>
      <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
        Регистрация
      </h2>
      <p class="mt-2 text-center text-sm text-gray-600">
        Создайте аккаунт для работы с системой
      </p>
    </div>

    <%= render "devise/shared/error_messages", resource: resource %>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "mt-8 space-y-6" }) do |f| %>
      <div class="rounded-md shadow-sm space-y-4 p-5 bg-white">
        <div class="grid grid-cols-2" style="gap: 16px;">
          <div>
            <%= f.input :first_name,
                        required: false,
                        autofocus: true,
                        label: "Имя",
                        input_html: { 
                          autocomplete: "given-name",
                          class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm"
                        },
                        label_html: { class: "block text-sm font-medium text-gray-700 mb-1" } %>
          </div>
          <div>
            <%= f.input :last_name,
                        required: false,
                        label: "Фамилия",
                        input_html: { 
                          autocomplete: "family-name",
                          class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm"
                        },
                        label_html: { class: "block text-sm font-medium text-gray-700 mb-1" } %>
          </div>
        </div>
        <div>
          <%= f.input :email,
                      required: false,
                      label: "Email",
                      input_html: { 
                        autocomplete: "email",
                        class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm"
                      },
                      label_html: { class: "block text-sm font-medium text-gray-700 mb-1" } %>
        </div>
        <div>
          <%= f.input :password,
                      required: false,
                      label: "Пароль",
                      hint: ("Минимум #{@minimum_password_length} символов, заглавные и строчные буквы, цифры" if @minimum_password_length),
                      input_html: { 
                        autocomplete: "new-password",
                        class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm"
                      },
                      label_html: { class: "block text-sm font-medium text-gray-700 mb-1" },
                      hint_html: { class: "mt-1 text-xs text-gray-500" } %>
        </div>
        <div>
          <%= f.input :password_confirmation,
                      required: false,
                      label: "Подтверждение пароля",
                      input_html: { 
                        autocomplete: "new-password",
                        class: "appearance-none relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded-md focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm"
                      },
                      label_html: { class: "block text-sm font-medium text-gray-700 mb-1" } %>
        </div>
      </div>

      <p class="text-xs text-gray-500 text-center">
        После регистрации вам будет назначена роль "Только просмотр".<br>
        Для получения дополнительных прав обратитесь к администратору.
      </p>

      <div>
        <%= f.button :submit, "Зарегистрироваться", class: "group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
      </div>
    <% end %>

    <p class="mt-2 text-center text-sm text-gray-600">
      Уже есть аккаунт?
      <%= link_to "Войти", new_session_path(resource_name), class: "font-medium text-blue-600 hover:text-blue-500" %>
    </p>
  </div>
</div>
