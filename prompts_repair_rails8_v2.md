# Промпты для GitHub Copilot (Grok Code Fast 1)
## Доработка раздела "Ремонт" в приложении учёта оборудования
**Rails 8.0.4**

---

## ПРОМПТ #1: Обновление таблицы "Ремонт" — удаление колонок, добавление номера заявки

```
В Rails 8.0.4 приложении учёта оборудования нужно обновить страницу списка оборудования в статусе "В ремонт" (раздел "Ремонт").

Текущая таблица содержит колонки:
- Чекбокс выбора
- Система (CUTE/FIDS/Zamar)
- Терминал
- Место установки
- Тип оборудования
- Модель
- Инвентарный номер
- Серийный номер
- Текущий статус
- Примечание
- Дата последнего действия (УДАЛИТЬ)
- Пользователь, который последний раз действовал (УДАЛИТЬ)

Требования:

1. УДАЛИТЬ отображение из таблицы две колонки:
   - "Дата последнего действия" (last_action_date)
   - "Пользователь" (last_changed_by или имя пользователя)

2. ДОБАВИТЬ новую колонку "Номер заявки":
   - Название: "Номер заявки"
   - Тип поля: текстовое поле (input type="text")
   - Это будет поле для ввода номера заявки на ремонт, которое можно заполнять перед оформлением акта
   - Данные должны сохраняться в новое поле модели оборудования (repair_ticket_number)
   - Значение можно редактировать прямо в таблице (inline edit) с использованием Turbo
   - При изменении отправлять PATCH запрос на обновление

3. Обновить контроллер RepairsController:
   - Убедиться, что в action#index исключены колонки last_action_date и last_changed_by из выборки
   - Использовать Turbo для обновления страницы после изменения номера заявки
   - Добавить action для обновления номера заявки:
     * PATCH /repairs/:id/update_ticket_number
     * Получает equipment_id и ticket_number
     * Обновляет поле repair_ticket_number
     * Возвращает Turbo Stream для обновления ячейки таблицы

4. Обновить модели оборудования (CuteEquipment, FidsEquipment, ZamarEquipment):
   - Добавить новое поле repair_ticket_number (string, nullable)
   - Использовать validates для валидации (если требуется)

5. Миграция БД (Rails 8.0.4 синтаксис):
   - Добавить колонку repair_ticket_number к таблицам:
     * cute_equipments
     * fids_equipments
     * zamar_equipments
   - Тип: string, null: true
   - Использовать новый синтаксис Rails 8: `add_column :table_name, :repair_ticket_number, :string`

6. View обновить (используя Rails 8 helpers и Turbo):
   - Удалить из таблицы колонки дата и пользователь
   - Добавить колонку с input полем для номера заявки
   - Использовать Tailwind CSS для стилизации
   - Использовать Turbo для inline edit без полной перезагрузки страницы
   - Input должен быть в form с автоматической отправкой при blur

7. Технические особенности Rails 8:
   - Использовать Turbo для реактивного обновления
   - Использовать .turbo_stream для ответов с сервера
   - Убедиться, что Turbo Drive не конфликтует с формами

Предоставь:
- Миграцию для новой колонки repair_ticket_number
- Обновлённый view таблицы оборудования (repairs/index.html.erb)
- Контроллер action для обновления номера заявки с Turbo Stream ответом
- Примеры кода для Rails 8.0.4
- Маршруты (config/routes.rb)
```

---

## ПРОМПТ #2: Модальное окно подтверждения акта с нумерованным списком

```
В Rails 8.0.4 приложении, в разделе "Ремонт", после выбора оборудования чекбоксами, пользователь нажимает кнопку "Оформить акт передачи в ремонт". 

Нужно обновить модальное окно подтверждения.

Требования:

1. Обновить текст заголовка:
   - Новый: "Количество выбранного оборудования: X"
   - Подставлять динамическое количество (X = количество выбранных элементов)

2. Добавить содержимое в модальное окно:
   - Ниже заголовка выводить НУМЕРОВАННЫЙ СПИСОК всего выбранного оборудования
   - Формат каждого элемента:
     "1. [Тип оборудования] — [Модель] — [Инвентарный №] — [Серийный №]"
     Пример:
     "1. Сканер — HP ScanJet — INV-001 — SN-12345"
     "2. Принтер посадочных талонов — Zebra — INV-002 — SN-67890"

3. Высота списка:
   - Если элементов много (больше 10-15), сделать скролируемым
   - max-height: 400px, overflow-y: auto
   - Использовать Tailwind CSS классы

4. Структура модального окна (используя Rails 8 модальные компоненты):
   +----------------------------------------------+
   | Оформление акта передачи в ремонт          | (X закрыть)
   +----------------------------------------------+
   | Количество выбранного оборудования: 30      |
   |                                              |
   | 1. CUTE — Сканер — HP ScanJet — INV-001    |
   | 2. FIDS — Принтер — Zebra — INV-002        |
   | ... (скролируемый список)                   |
   | 30. Zamar — DSM — Nexus — INV-030          |
   |                                              |
   | [ Отменить ]  [ Подтвердить акт ]          |
   +----------------------------------------------+

5. Функциональность после подтверждения:
   - При нажатии "Подтвердить акт":
     * Для каждого выбранного оборудования:
       - Если привязано к месту установки:
         * Отвязать оборудование (обнулить внешний ключ установки)
         * Логировать в audit log: "Отвязано от места установки"
       - Изменить статус на "Ожидается из ремонта" (waiting_repair)
       - Логировать: "Статус изменён на 'Ожидается из ремонта'"
     * Создать RepairBatch/RepairAct
     * Связать оборудование с актом
     * Перенаправить на страницу просмотра акта

6. JavaScript (с использованием ES6 и Turbo):
   - Собирать выбранные элементы из чекбоксов
   - Передавать список в модальное окно через fetch API (или Turbo)
   - Рендерить нумерованный список
   - Обработка подтверждения (POST запрос с equipment_ids)

7. Использовать в Rails 8:
   - Turbo для асинхронных операций
   - Stimulus для управления модальным окном
   - .turbo_stream для обновления страницы после успешного действия

Предоставь:
- Обновленный view модального окна (partial)
- JavaScript/Stimulus контроллер для управления модальным окном
- Контроллер action для создания акта и массового изменения статусов
- Логика отвязывания оборудования от мест установки
- Примеры для Rails 8.0.4
```

---

## ПРОМПТ #3: Страница просмотра акта с таблицей и подписью сторон

```
В Rails 8.0.4 приложении, после создания акта передачи в ремонт, пользователь видит страницу просмотра этого акта.

Нужно обновить вывод информации на этой странице.

Требования:

1. Структура страницы акта:
   
   Верхняя часть:
   - Номер акта
   - Дата формирования акта
   - Время формирования (опционально)
   - Пользователь, создавший акт (ФИО)

2. Блок "Оборудование в акте":
   - Использовать ТАБЛИЦУ с первой колонкой "№" (номер по порядку)
   - Колонки таблицы (в порядке слева направо):
     * № (номер по порядку: 1, 2, 3, ...) — узкая колонка, по центру
     * Тип оборудования (equipment_type) — русское название из enum
     * Модель (model)
     * Инвентарный номер (inventory_number)
     * Серийный номер (serial_number)
     * Номер заявки (repair_ticket_number) — если заполнено, иначе "—"
   
   Пример таблицы:
   ```
   +----+----------------------------------+------------------+----------+----------+---------+-------+
   | № | Тип оборудования                 | Модель           | Инв. №   | Сер. №   | Заявка  | Сист. |
   +----+----------------------------------+------------------+----------+----------+---------+-------+
   | 1  | Сканер                          | HP ScanJet Pro   | INV-001  | SN-12345 | REP-001 |
   | 2  | Принтер посадочных талонов      | Zebra ZT410      | INV-002  | SN-67890 | REP-002 |
   | 3  | DSM                             | Nexus Pro        | INV-003  | SN-11111 | —       |
   +----+----------------------------------+------------------+----------+----------+---------+-------+
   ```

3. Удалить из таблицы:
   - Терминал (terminal)
   - Место установки (installation)
   
4. Добавить в таблицу:
   - Первая колонка: № (номер по порядку 1, 2, 3, ...)
   - Тип оборудования (equipment_type) — русское название из enum
   - Номер заявки (repair_ticket_number) — если заполнено, иначе "—"

5. Нижняя часть акта (подписи):
   
   Блок 1: "Оборудование сдал"
   ```
   Оборудование сдал:
   
   ФИО: ___________________________    Подпись: __________
   
   Подразделение: ________________    Дата: ___________
   ```
   
   Блок 2: "Оборудование принял"
   ```
   Оборудование принял:
   
   ФИО: ___________________________    Подпись: __________
   
   Подразделение: ________________    Дата: ___________
   ```
   
   Требования:
   - Два отдельных блока (один под другим или напротив)
   - Поля для рукописного заполнения (пустые строки)
   - Tailwind CSS для разметки (grid или flex)
   - Выглядит как официальный документ

6. Кнопки:
   - "Экспорт в Excel" (верхний правый угол)
   - "Печать" (для браузерной печати)
   - "Вернуться к списку" (навигация)

7. Печать:
   - Оптимизированное форматирование для печати на бумаге A4
   - Использовать @media print в CSS

8. Дизайн:
   - Tailwind CSS
   - Официальный документ вид
   - Чистое и аккуратное оформление

Предоставь:
- View страницы просмотра акта (repairs/acts/show.html.erb)
- Контроллер action для отображения акта
- Helper методы для форматирования (русские названия типов)
- Tailwind CSS стили
- CSS для печати (@media print)
```

---

## ПРОМПТ #4: Экспорт акта в Excel с таблицей и подписями

```
В Rails 8.0.4 приложении нужно реализовать экспорт акта передачи в ремонт в Excel (.xlsx).

Требования:

1. Структура файла Excel:
   
   Строка 2: "АКТ ПЕРЕДАЧИ ОБОРУДОВАНИЯ В РЕМОНТ" (заголовок, жирный, размер 14, по центру)
   Строка 3: Пустая
   Строка 4: "Номер акта: [номер]"
   Строка 5: "Дата: [дата формирования]"
   Строка 6: "Время: [время формирования]"
   Строка 7: "Создал: [ФИО пользователя]"
   Строка 8: Пустая
   Строка 9: "Количество оборудования: [количество]"
   Строка 10: Пустая
   
   Строка 11: "СПИСОК ОБОРУДОВАНИЯ" (жирный, размер 12, по центру)
   Строка 12: Пустая
   
   Затем ТАБЛИЦА с колонками:
   Строка 13 (заголовок таблицы):
   | № | Тип оборудования | Модель | Инв. номер | Сер. номер | Номер заявки |
   
   Строка 14 и далее (данные):
   | 1 | Сканер | HP ScanJet Pro | INV-001 | SN-12345 | REP-001 |
   | 2 | Принтер посадочных талонов | Zebra ZT410 | INV-002 | SN-67890 | REP-002 |
   | 3 | DSM | Nexus Pro | INV-003 | SN-11111 | — |
   
   После таблицы (конец данных + 3 строки):
   Пустая строка
   
   Затем:
   Строка N: Пустая
   Строка N+1: "ПОДПИСИ И РЕКВИЗИТЫ" (жирный, размер 12, по центру)
   Строка N+2: Пустая
   Строка N+3: "Оборудование сдал:"
   Строка N+4: "ФИО: _________________________ | Подпись: ______________"
   Строка N+5: "Подразделение: _______________ | Дата: ___________"
   Строка N+6: Пустая
   Строка N+7: "Оборудование принял:"
   Строка N+8: "ФИО: _________________________ | Подпись: ______________"
   Строка N+9: "Подразделение: _______________ | Дата: ___________"

2. Форматирование Excel:
   - Шрифт: Arial, размер 11 (по умолчанию)
   - Заголовок "АКТ": жирный, размер 14, по центру
   - Заголовки разделов: жирный, размер 12, по центру
   - Заголовок таблицы: жирный, размер 11, по центру
   - Строки таблицы: обычный текст, размер 11
   - Ширина колонок: автоматическая или с разумными значениями
   - Поля для подписей: минимум 15 символов (_____)
   - Ориентация: книжная (portrait)
   - Границы таблицы: простые границы

3. Gem для экспорта:
   - Использовать gem 'write_xlsx' (или gem 'caxlsx' / gem 'axlsx_rails')
   - Убедиться, что gem совместим с Rails 8.0.4

4. Контроллер:
   - Action: repairs/acts#export или repairs/acts#export_to_excel
   - Формат: respond_to { |format| format.xlsx { ... } }
   - MIME type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
   - Имя файла: "Акт_[номер акта]_[дата].xlsx"
   - Пример: "Акт_ACT-2026-00123_2026-01-27.xlsx"

5. View/Template для Excel:
   - Использовать view template (.xlsx.erb) или Builder pattern
   - Построить структуру файла в контроллере или view
   - Использовать правильный синтаксис для write_xlsx

6. Кнопка в view:
   - <%= link_to "Экспорт в Excel", repair_act_path(@repair_act, format: :xlsx), class: "..." %>

7. Маршруты (config/routes.rb):
   - GET /repairs/acts/:id (show)
   - GET /repairs/acts/:id.xlsx (export)

Требования для Rails 8:
- Использовать новый синтаксис render в контроллере
- Убедиться в совместимости gem с Rails 8
- Использовать moderne Ruby синтаксис (safe navigation и т.п.)

Предоставь:
- Контроллер action для экспорта в Excel
- Код построения файла (используя write_xlsx или подобный)
- Форматирование ячеек (bold, размер, выравнивание)
- View template для Excel (если требуется)
- Маршруты
- Примеры использования
```

---

## ПРОМПТ #5: История ремонта — добавление ссылки на список актов

```
В Rails 8.0.4 приложении, в разделе "История ремонта", нужно добавить навигацию к списку актов.

Требования:

1. На странице "История ремонта" добавить ссылку:
   - Название: "Просмотр списка актов" или "Список актов передачи в ремонт"
   - Размещение: в верхней части страницы, рядом с фильтрами или выше них
   - Визуально: кнопка или ссылка с иконкой
   - Использовать Tailwind CSS для стилизации

2. Страница со списком актов (Repairs::ActsController#index):
   - Таблица со всеми актами:
     * Номер акта
     * Дата создания (отсортировано по новым сверху)
     * Время создания (опционально)
     * Пользователь, создавший акт
     * Количество оборудования в акте
     * Статус акта (опционально)
     * Действие (кнопка "Просмотр")
   - Пагинация: 20-30 актов на странице (использовать pagy gem в Rails 8)
   - Сортировка: по дате (новые сверху)
   - Фильтры (опционально):
     * По дате (диапазон)
     * По пользователю (кто создал)

3. Пример строки в таблице:
   ```
   | АКТ-2026-00123 | 27.01.2026 15:23 | Иван Петров | 30 | Активный | [Просмотр] |
   ```

4. Навигация:
   - На странице просмотра акта: ссылка "Вернуться к списку актов" или "К списку"
   - На странице истории ремонта: ссылка "К списку актов"
   - Использовать <%= link_to %> helpers

5. Маршруты (config/routes.rb):
   - GET /repairs/acts — список актов
   - GET /repairs/acts/:id — просмотр акта
   - Структура: namespace :repairs или resources :acts внутри repairs namespace

6. Контроллер:
   - RepairsController#acts_index или ActsController#index
   - Загружать акты с пользователем через includes (избежать N+1)
   - Применить пагинацию (pagy в Rails 8)
   - Opционально: фильтры через ransack

7. View:
   - Tailwind CSS стили
   - Таблица с чистым оформлением
   - Responsive дизайн
   - Links на просмотр каждого акта

8. Технические детали Rails 8:
   - Использовать современный синтаксис (safe navigation, pattern matching где возможно)
   - Убедиться в совместимости паджинации (pagy или kaminari)
   - Использовать Turbo если требуется быстрая загрузка

Предоставь:
- Обновленный view истории ремонта (repairs/history/index.html.erb) с ссылкой
- Контроллер ActsController#index с логикой загрузки и пагинации
- View списка актов (repairs/acts/index.html.erb)
- Маршруты (config/routes.rb) с примерами
- Helper методы для форматирования дат и статусов
- Примеры для Rails 8.0.4
```

---

## 📋 Последовательность внедрения для Grok Code Fast 1:

1. **ПРОМПТ #1** → Обновление таблицы (удалить колонки, добавить номер заявки)
2. **ПРОМПТ #2** → Модальное окно (нумерованный список + отвязывание)
3. **ПРОМПТ #3** → Страница акта (таблица с № + подписи)
4. **ПРОМПТ #4** → Экспорт в Excel (таблица + форматирование)
5. **PROMПТ #5** → История ремонта (навигация на список актов)

---

## 🛠️ Технический стек для Rails 8.0.4:

```ruby
# Gemfile (добавить/обновить)
gem 'write_xlsx'        # или caxlsx / axlsx_rails для Excel экспорта
gem 'pagy'              # пагинация (рекомендуется для Rails 8)
gem 'ransack'           # опционально для фильтров
gem 'turbo-rails'       # встроен в Rails 8 по умолчанию
gem 'stimulus-rails'    # встроен в Rails 8 по умолчанию
```



---

**Статус:** ✅ Готово для GitHub Copilot (Grok Code Fast 1)  
**Rails версия:** 8.0.4  
**Дата обновления:** 27 января 2026  
**Версия промптов:** 4.1 (исправлена таблица вместо нумерованного списка в ПРОМПТЕ #3)
